# Build-time variable: set it locally and as a GitHub Actions repository variable.
NEXT_PUBLIC_BASE_API_URL=https://api.example.com

# Server-side variables used by docker-compose.prod.yml.
REGISTRY_IMAGE=ghcr.io/fanete2021/notionfront
IMAGE_TAG=latest
BIND_ADDRESS=127.0.0.1
HOST_PORT=3000
