import { LoginPromo } from '@/widgets/auth-promo';
import { LoginPanel } from '@/widgets/login-panel';

export const LoginPage = () => {
  return (
    <>
      <LoginPromo />
      <LoginPanel />
    </>
  );
};
